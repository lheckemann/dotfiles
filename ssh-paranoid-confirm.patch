diff --git a/readpass.c b/readpass.c
index 05c8cac..040a425 100644
--- a/readpass.c
+++ b/readpass.c
@@ -183,8 +183,7 @@ ask_permission(const char *fmt, ...)
 		 * Accept empty responses and responses consisting
 		 * of the word "yes" as affirmative.
 		 */
-		if (*p == '\0' || *p == '\n' ||
-		    strcasecmp(p, "yes") == 0)
+		if (strcasecmp(p, "yes") == 0)
 			allowed = 1;
 		free(p);
 	}
